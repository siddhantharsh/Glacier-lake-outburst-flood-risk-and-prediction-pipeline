# GLOF Project Data Store

This directory holds the processed and intermediate datasets used throughout the GLOF risk modeling workflow. Files include engineered features, imputed datasets, and model outputs generated by the notebooks in the parent directory.